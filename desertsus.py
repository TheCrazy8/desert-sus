import time
import random
import tkinter as tk

duration_seconds = 28800  # 8 hours
start_time = time.time()  # Record the start time
facing = 1 #value for direction of bus
def veeroff():
  direction = pickrandom(0,1)
  if direction == 0:
    


while time.time() - start_time < duration_seconds:
    
    time.sleep(1)
